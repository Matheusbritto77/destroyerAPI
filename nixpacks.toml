[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["bun", "nodejs_20"]

[phases.install]
cmds = ["bun install --frozen-lockfile"]

[phases.build]
cmds = ["bun run check"]

[start]
cmd = "bun run start"
